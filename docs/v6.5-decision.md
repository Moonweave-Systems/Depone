# V6.5 Frontier Dispatch Decision

Decision: keep

Command used to verify frontier dispatch:

```bash
python scripts/dispatch_frontier.py --self-test
```

Dogfood commands:

```bash
python scripts/dispatch_frontier.py --frontier out/v6/v32-semantic-dogfood --out out/v6.5/v32-semantic-dogfood
python scripts/dispatch_frontier.py --resume out/v6.5/v32-semantic-dogfood
```

Generated dogfood values:

- `run_id`: `v32-semantic-dogfood`
- `status`: `prepared`
- `resume_state`: `resumable`
- `packet_id`: `v6-frontier-0001-release_decision`
- `phase_id`: `release_decision`
- `worker_ids`: `decision-worker`

This decision covers emit-only dispatch preparation from one deterministic V6
frontier. It does not claim worker execution, worker-result review for
`release_decision`, multi-result fan-in, Codex CLI execution, OMX execution,
subagent spawning, worktree merging, commits, pushes, dependency installation,
production deployment, external messaging, secret access, or autonomous
workflow completion.
